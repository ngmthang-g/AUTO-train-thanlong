THAN LONG AUTO NEWCORE v1.1.0 - INTEGRATED ACCEPTANCE STATIC AUDIT
Date: 2026-08-15

ARCHITECTURE
- Scanner is read-only.
- ReadGameSnapshot remains BridgeCommand=5.
- ProveHookActionEnvelope=6 is infrastructure-only and must never contain a gameplay action call.
- No CreateRemoteThread.
- No il2cpp_thread_attach.
- No Sleep( in NewCore source.
- kGameplayMutationEnabled must be exactly false.
- kHarmlessInfrastructureProofEnabled must be exactly true.
- ActionQueueScaffold::kCapacity must be exactly 1.
- No gameplay action is emitted from scanner or capability resolver.

HARMLESS ACTION ENVELOPE
- Runs through the existing WH_GETMESSAGE callback only.
- Re-proves Unity main thread before completion.
- Allowed side effect: bridge-local monotonically increasing proof counter only.
- Returns token, sequence, callback TID, managed current/main IDs.
- Controller must hold queue active until POST verifies all returned evidence.
- Source must contain no ClickNPC, HandleClickEvent, StartAutoFight, RequestSellItem, StartPath, StopPath or direct runtime_invoke.
- Failure clears queue and bridge fail-closed.

ACTION CAPABILITY PROBE
- Action names may appear only as metadata strings/evidence; their presence is not permission to invoke.
- Probe uses class/method/field/type metadata inspection only.
- Probe source must contain no runtime_invoke, InvokeObject, InvokeScalar or BridgeCommand dispatch.
- UI metadata support and NPC/inventory metadata support are reported independently.
- Dynamic Lua combat and sell callbacks stay DONOR_ONLY / UNRESOLVED.
- No live UIButton/UIToggle pointer is acquired or cached by the probe.

AUTOMATED CONTROL TEST
- FSM model.
- SafetyGuard PRE conditions.
- identity drift + map-transition block.
- ActionQueue queued/active serialization + cancel.
- scanner watchdog 3-failure trip/reset.
- gameplay dispatcher remains mutation-locked.
- infrastructure dispatcher accepts only HarmlessInfrastructureProof.
- harmless POST positive case + bad token + stale sequence + wrong callback thread + managed-thread mismatch.
- Revive/combat gameplay postcondition model checks.
- Expected integrated self-test count: 48.

CI REQUIRED
- ControlSelfTest compile + 48/48 PASS.
- metadata-only capability audit PASS.
- harmless-envelope forbidden-token audit PASS.
- Windows Zig 0.15.2 build.cmd PASS.
- Bridge LoadLibrary self-test PASS.
- Controller EXE build PASS.
- EXE/DLL artifact verify/upload PASS.

RUNTIME POLICY
- No phase-by-phase manual testing.
- Command 6 still needs one later integrated live acceptance on the client; this code/CI checkpoint alone does not unlock gameplay mutation.
