THAN LONG AUTO TRAIN v0.8.2 - BUILD/VERIFY STATUS
=================================================

STATIC GAME-BUILD VERIFICATION
- GameAssembly image base: 0x180000000
- NPC ClickNPC(int) predicate disassembled on the supplied binary.
- Proven identifier: ClickNPC compares its argument with GNPC/GMovingNPC +0xC0 (ResID), not RoleID.
- GNPC/GMovingNPC get_ResID() = RVA 0x68EE70 and returns [this+0xC0].
- New guarded RVAs checked against the supplied GameAssembly first bytes:
  GetNearestNPC(int)        0x673A80
  GetNearestNPC()           0x673A90
  ClickNPC(int)             0x66ADC0
  SessionData.get_NPCs()    0x6F3DD0
  SessionData.get_MovingNPCs() 0x6F3D80
  GNPC.get_ResID()          0x68EE70

AUTO ARCHITECTURE CHECK
- TriggerAutoMenu() is only called in the final-at-train-spot combat branch.
- Navigation/mount/portal recovery does not call TriggerAutoMenu().
- If AutoFight remains ON while leaving a spot, navigation waits for real AUTO -> Dung confirmation before AutoPath/mount/portal commands resume.
- On arrival, both modes down-mount first and wait 4 seconds before AutoMenu can activate combat.
- AutoSetFlag(int RangerAuto) was rejected as a combat-mode selector after native disassembly; it is used for the auto-fight range circle.

REMOTE WORKER
- src/remote_worker.S assembled successfully as x86-64 Windows COFF with clang target x86_64-w64-windows-gnu.
- 5th native argument is placed at Win64 stack slot [rsp+0x20].

LOCAL FULL WINDOWS LINK
- NOT performed in this Linux container: a complete Windows SDK/MinGW/Zig linker environment is not installed.
- A fake Win32-header syntax pass reached the entire source; its diagnostics are missing Win32 declarations from the fake headers, with no diagnostics in the modified NPC/AUTO regions.
- The included build.cmd and GitHub Actions workflow are the authoritative full Windows -Wall -Wextra -Werror build path.

RUNTIME
- Not claimed as tested against a live game/server from this environment.
